# 💻 Entrega II – CSS3 Design System

## 🎯 Objetivo
Aplicar os conceitos de **CSS3** para transformar a estrutura HTML da Entrega I em uma **interface visual profissional, responsiva e acessível**.

Este projeto implementa um **Design System completo**, com tokens de design, grid de 12 colunas, navegação responsiva e componentes interativos.  
Desenvolvido como parte da disciplina de **Tecnologia da Informação**.

---

## 🧱 Especificações Técnicas Implementadas

### 🎨 Sistema de Design
- Uso de **variáveis CSS customizadas** (`tokens.css`);
- **Paleta de 8+ cores** (primárias, secundárias, neutras e feedback);
- **Tipografia hierárquica** com 5+ tamanhos de fonte;
- **Sistema de espaçamento modular** (8, 16, 24, 32, 48 e 64px).

### 🧩 Leiaute Responsivo
- **Grid customizado de 12 colunas** com classes utilitárias (`.col-*`, `.sm:col-*`, etc.);
- **5 breakpoints responsivos:** `xs`, `sm`, `md`, `lg`, `xl`;
- Estrutura principal com **CSS Grid** e alinhamentos com **Flexbox**;
- Layouts otimizados para desktop, tablet e mobile.

### 🧭 Navegação
- **Menu principal responsivo** com submenu dropdown;
- **Menu mobile tipo hambúrguer** com controle via JavaScript e atributos ARIA.

### 🧠 Componentes de Interface
- Sistema de **cards responsivos** para projetos;
- **Botões** com estados visuais (`hover`, `focus`, `active`, `disabled`);
- **Formulários estilizados** com **validação visual** (`:valid`, `:invalid`, `.is-invalid`);
- **Componentes de feedback:** alerts, toasts e modal;
- **Badges e tags** para categorização e destaque.

---

## 🗂️ Estrutura de Pastas
